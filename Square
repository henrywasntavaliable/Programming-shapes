"""Write a function called `square_dimensions_full` that:
- Asks the user for the x and y coordinates of the top-left corner of a square.
- Assume a fixed width and height of 100.
- Prints all four coordinates: top-left, top-right, bottom-left, and bottom-right.

Solution Example:
Enter the x coordinate of the top-left corner: 50
Enter the y coordinate of the top-left corner: 50
Top-left coordinates: 50 50
Top-right coordinates: 150 50
Bottom-left coordinates: 50 150
Bottom-right coordinates: 150 150
"""

def square_thing_full():
    width = 100
    height = 100
    x = float(input("Please enter X"))
    y = float(input("Please enter Y"))
    top_left = x + y
    top_right = x + width, y 
    bottom_right = x + width, y + height
    bottom_left = x, y + height
    print(top_right,top_left,bottom_right,bottom_left)
    return (x,y,width,height)

square_thing_full()

